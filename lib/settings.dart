import 'package:flutter/cupertino.dart';

class settings extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Text('Settings page. A mock-up for display purpose. Not finalize!!');
  }

}
